@model List<MPLSCoffee.Web.Models.BlogPost>
@{
    ViewData["Title"] = "Blog";
}

<h2>Blog Posts</h2>
<ul>
    @foreach (var post in Model)
    {
        <li><a href="@Url.Action("Details", "Blog", new { slug = post.Slug })">@post.Title</a></li>
    }
</ul>